export interface User {
  text: string;
  value: string;
}

export interface Users {
  user: string;
  userSecondName: string;
  userMail: string;
  selectedGender: string;
  pwd: string;
  isChecked: boolean;
  id: number;
}
